import 'package:flutter/material.dart';

class DeleteAndLogoutCard extends StatelessWidget {
  const DeleteAndLogoutCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
